@model  List<CTS_Models.BeltScale>
@using PagedList.Mvc;
@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@if ((Model.Count > 0) && (Model != null))
{
<h2>Конвейерные весы на объектах</h2>
    <br />
       <input type="button" name="AddButton" class="btn btn-primary" value="Добавить новые весы" onclick="location.href='@Url.Action("BeltScalesAdd", "Dictionary")'" />
    <br />
    <br />

    if ((Model.Count > 0) && (Model != null))
    {
    <table class="table table-striped table-hover ">
        <thead>
            <tr>
                <th>Наименование на русском</th>
                <th>Наименование на английском</th>
				<th>Наименование на казахском</th>
                <th>Расположение</th>
                <th>Начало конвейера</th>
                <th>Окончание конвейера</th>

                <th> </th>
            </tr>
        </thead>
        <tbody>
            @for (int i = 0; i < Model.Count; i++)
            {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => Model[i].Name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => Model[i].NameEng)
                </td>
				<td>
					@Html.DisplayFor(modelItem => Model[i].NameKZ)
				</td>
                <td>
                    @Html.DisplayFor(modelItem => Model[i].Location.LocationName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => Model[i].FromInnerDest.Name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => Model[i].ToInnerDest.Name)
                </td>
                <td>
                    <input type="button" name="EditButton" class="btn btn-primary btn-xs" value="Изменить" onclick="location.href='@Url.Action("BeltScalesEdit", "Dictionary", new { Id = Model[i].ID })'" />
                </td>
            </tr>
            }
        </tbody>
    </table>
    }
}